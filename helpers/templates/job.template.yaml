apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .JobName }}
  labels: 
    bigpicture: bpctl
    release: {{ .JobReleaseLabel }}
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        bigpicture: bpctl
        release: {{ .JobReleaseLabel }}
    spec:
      securityContext:
        fsGroup: 65534
        runAsGroup: 65534
        runAsUser: 65534
      restartPolicy: Never
      containers:
        - name: bpctl
          image: harbor.nbis.se/sda/bpctl:latest
          imagePullPolicy: Always
          args: {{ .JobArgs }}
          env:
            - name: USER_ID
              value: {{ .UserID }}
            - name: DATASET_ID
              value: {{ .DatasetID }}
            - name: DATASET_FOLDER
              value: {{ .DatasetFolder }}
            - name: SSL_CA_CERT
              value: "{{ .SslCaCert }}"
            - name: CLIENT_API_HOST
              value: {{ .ClientApiHost }}
            - name: CLIENT_ACCESS_TOKEN
              value: {{ .ClientAccessToken }}
            - name: MAIL_UPLOADER_NAME
              value: "{{ .MailUploaderName }}"
            - name: MAIL_UPLOADER_ORGANIZATION_NAME
              value: "{{ .MailUploaderOrganizationName }}"
            - name: MAIL_UPLOADER
              value: {{ .MailUploader }}
            - name: MAIL_ADDRESS
              value: {{ .MailAddress }}
            - name: MAIL_PASSWORD
              value: {{ .MailPassword }}
            - name: MAIL_SMTP_HOST
              value: {{ .MailSmptHost }}
            - name: MAIL_SMTP_PORT
              value: "{{ .MailSmptPort }}"
            {{- if .WithDB }}
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_PORT
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_NAME
            - name: DB_SCHEMA
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_SCHEMA
            - name: DB_SSL_MODE
              valueFrom:
                secretKeyRef:
                  name: {{ .DbSecretName }}
                  key: DB_SSL_MODE
            - name: DB_CA_CERT
              value: "/.secrets/tls/ca.crt"
            - name: DB_CLIENT_CERT
              value: "/.secrets/tls/tls.crt"
            - name: DB_CLIENT_KEY
              value: "/.secrets/tls/tls.key"
          {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: data-volume
              mountPath: /data
            {{- if .WithXml }}
            - name: xml-data-{{ .JobName }}
              mountPath: /data/xml
            {{- end }}
            - name: tls
              mountPath: /.secrets/tls/
      volumes:
        - name: data-volume
          emptyDir: {}
        {{- if .WithXml }}
        - name: xml-data-{{ .JobName }}
          secret:
            defaultMode: 288
            secretName: {{ .MailXmlSecretName }}
        {{- end }}
        - name: tls
          secret:
            defaultMode: 288
            secretName: {{ .CertSecretName }}
